#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#define UNUSED __attribute__((unused))

#include <servo.h>


int main(UNUSED int argc, UNUSED char const *argv[]) {
  while(1){
    // servo_enable(0, 1);
    // servo_set(0, 80);

    // servo_enable(1, 1);
    // servo_set(1, 40);

    // servo_enable(2, 1);
    // servo_set(2, 40);
  }
}